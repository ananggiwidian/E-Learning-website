import DaftarNotification from "../Fragments/DaftarNotification";
import LoginHeader from "../Fragments/LoginRegisterHeader";

const DaftarNotificationPages = () => {
	return (
		<div>
			<LoginHeader />
			<DaftarNotification />
		</div>
	);
};

export default DaftarNotificationPages;
